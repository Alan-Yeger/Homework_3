#include "../includes/Circulo.hpp"

void Circulo::setX(float newX) {
    x = newX;
};

void Circulo::setY(float newY) {
    y = newY;
};

void Circulo::setR(float newR) {
    r = newR;
};

float Circulo::getX() const {
    return x;
};

float Circulo::getY() const {
    return y;
};

float Circulo::getR() const {
    return r;
};