#include "../includes/Rectangulo.hpp"

void Rectangulo::setX(float newX) {
    x = newX;
};

void Rectangulo::setY(float newY) {
    y = newY;
};

void Rectangulo::setW(float newW) {
    w = newW;
};

void Rectangulo::setH(float newH) {
    h = newH;
};

float Rectangulo::getX() const {
    return x;
};

float Rectangulo::getY() const {
    return y;
};

float Rectangulo::getW() const {
    return w;
};

float Rectangulo::getH() const {
    return h;
};